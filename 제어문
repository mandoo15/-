# 제어문
프로그램을 제작할 때 조건에 따라 실행 순서를 바꾸거나 반복적인 실행을 하도록 프로그램 흐름을 제어하는 문장

조건문 / 선택문 / 반복문 / 기타문

!!! 조건문 !!! // if, else, if else
조건식에 나오는 결괏값이 true, false에 따라 실행문을 제어하는 경우

if문 - 조건식의 값이 참인 경우 실행문을 실행할 때 사용
ex )
var num = 5
if( num < 10 ) {
  document.write('num은 10보다 작아요') // document 즉 html 문서에 해당 메시지가 출력됨을 의미
}

ex ) 사용자 입력값 받아서 처리
var num = prompt('숫자를 입력하세요')

if(num < 10) {
  document.write('num은 10보다 작아요')
}

if-else문 - 조건식의 값이 참값일 경우 또는 거짓값일 경우에 실행되는 실행문이 다른 경우
ex )
var age = prompt('나이를 입력하세요')

if(age > 20 && age < 40) {
  document.write('입장을 환영합니다!')
else {
  document.write('입장이 허락되지 않았습니다!')
}

if-else if문 - 조건1식의 값이 참인 경우 실행문을 정해 거짓값인 경우의 수를 다시 모아서 재차 조건을 걸어 참일 경우와 거짓일 경우를 나누어 다시 실행문을 실행할 때 사용된다.
1차 참인 경우를 제외한 모든 경우의 수는 여러 번의 조건을 걸쳐 실행문을 찾을 수 있게 한다.


!!! 선택문 !!! // switch
다양한 조건이 제시되었을 때 선택값에 따라 실행문을 선택해 실행하도록

!!! 반복문 !!! // for
동일한 원리를 가진 실행문을 조건이 성립될 때까지 계속 반복적을 실행하도록 하는 제어문

!!! 반복문 !!! / while, do-while
for문은 for문 내에서 작동되는 카운터에 의해 실행문이 반복된다
while / do-while은 반복문 외부에서 기준값을 가져와 특정 조건이 달성될 때까지 실행문이 반복됨
